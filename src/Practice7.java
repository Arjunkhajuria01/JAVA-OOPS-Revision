// ### **Goal:** Practice creating subclasses, method overriding, and overloading.
//
//**Practice Questions:**
//
//1. Create subclasses `Driver` and `Passenger` extending `User`.
//    - `Driver`: add method `addRide(Ride ride)`
//    - `Passenger`: add method `requestRide(Ride ride)`
//2. Create Driver and Passenger objects. Call their respective methods.
//3. **Method Overloading:** In `User` class, create `displayInfo()` method:
//    - No parameter → basic info
//    - With parameter `boolean full` → show full info including role
//4. **Method Overriding:** Override `displayInfo()` in Driver class to also print rides offered.
//5. **Constructor Overloading:** In `Ride` class, create 3 constructors:
//    - Default constructor
//    - Pickup + Drop only
//    - Pickup + Drop + Time + Seats
//6. **Challenge:** Create a `PremiumDriver` subclass that extends Driver and overrides `addRide()` to apply a **priority flag**for certain rides.



public class Practice7 {
    public static void main(String[] args) {

    }
}
